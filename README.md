# Pair Trading Bot

This is a Python project for a **pair trading strategy** example.  
It uses data from Yahoo Finance, runs a cointegration test, and simulates simple trades.

## How to Run
1. Create a virtual environment
2. Install dependencies from `requirements.txt`
3. Run:
   ```bash
   python run_backtest.py